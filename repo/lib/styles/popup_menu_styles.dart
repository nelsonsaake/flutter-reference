import 'package:flutter/material.dart';

class PopupMenuStyles {
  static final shape = RoundedRectangleBorder(
    borderRadius: BorderRadius.circular(15),
  );

  static const offset = Offset(64, 0);
}
